{
  "name": "whenwe",
  "version": "0.1.0",
  "private": true,
  "description": "우리 언제? — 기간 기반 모임 시간 조율 서비스",
  "scripts": {
    "web:dev": "pnpm --filter web dev",
    "api:dev": "pnpm --filter api start:dev",
    "lint": "pnpm -r lint",
    "build": "pnpm -r build"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0"
}
